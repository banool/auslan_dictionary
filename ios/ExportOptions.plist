<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>method</key>
	<string>app-store-connect</string>
	<key>teamID</key>
	<string>9N3SNHTGL7</string>
	<!-- Automatic signing: the app uses Sign In with Apple + Associated Domains,
	     so the profile must carry those entitlements. Automatic signing
	     regenerates such a profile on the fly; a hand-pinned profile does not.
	     upload.sh clears revoked/expired certs first so the automatic resolver
	     cannot select an invalid one ("Signing certificate is invalid"). -->
	<key>signingStyle</key>
	<string>automatic</string>
	<key>uploadSymbols</key>
	<true/>
</dict>
</plist>
